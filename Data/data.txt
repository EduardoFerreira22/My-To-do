CREATE TABLE IF NOT EXISTS PRIORIDADES (
	ID INTEGER PRIMARY KEY AUTOINCREMENT,
	PRIORIDADE TEXT NOT NULL,
	COR TEXT
);

CREATE TABLE IF NOT EXISTS CATEGORIAS (
	ID INTEGER PRIMARY KEY AUTOINCREMENT,
	CATEGORIA TEXT NOT NULL,
	COR TEXT
);

CREATE TABLE IF NOT EXISTS TAREFAS (
	TASK_ID INTEGER PRIMARY KEY AUTOINCREMENT,
	STATUS_TASK TEXT DEFAULT NULL,
	DESCRICAO TEXT NOT NULL,
	ID_PRIORIDADE INTEGER,
	ID_CATEGORIA INTEGER,
	ADD_AO_MEU_DIA INTEGER DEFAULT 0,
	LEMBRAR INTEGER DEFAULT 0,
	DATA_LEMBRAR DATE,
	REPETIR TEXT,
	ANOTACOES TEXT,
	DATA_CRIACAO DATETIME DEFAULT CURRENT_TIMESTAMP,
	DATA_UPDATE DATETIME,
	FOREIGN KEY (ID_PRIORIDADE) REFERENCES PRIORIDADES(ID),
	FOREIGN KEY (ID_CATEGORIA) REFERENCES CATEGORIAS(ID)
);

INSERT INTO PRIORIDADES(PRIORIDADE) VALUES ('Circunstanciais');
INSERT INTO PRIORIDADES(PRIORIDADE) VALUES ('Importante');
INSERT INTO PRIORIDADES(PRIORIDADE) VALUES ('Urgente');

INSERT INTO CATEGORIAS(CATEGORIA) VALUES ('Meu Dia');
INSERT INTO CATEGORIAS(CATEGORIA) VALUES ('Todas');
INSERT INTO CATEGORIAS(CATEGORIA) VALUES ('Concluída');

